apiVersion: v1
kind: Service
metadata:
  name: demo-frontend-lb
  namespace: demo-guard
spec:
  type: LoadBalancer
  selector:
    app: demo-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
